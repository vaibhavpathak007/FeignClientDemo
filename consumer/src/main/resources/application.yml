server:
  port: 9092

spring:
  application:
    name: consumer-service
  cloud:
    config:
      enabled: false

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
#    hostname: localhost

logging:
  level:
    web: INFO



